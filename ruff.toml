# Ruff — keep CI lint focused on test infrastructure (legacy app debt excluded).
target-version = "py311"
line-length = 100

[lint]
select = ["E9", "F63", "F7", "F82", "F401", "F811"]

[lint.per-file-ignores]
"tests/**" = ["F401"]
